<div id="comments">

      <%= render 'comments/form', comment: @comment %>

      <% @blog.comments.each do |comment| %>
      <p>
  	    <%= blog.user.email %> said...<br />
  	    <%= comment.comment_entry %>
  		</p>
      <% end %>
      
  </div>

<%= link_to 'Edit', edit_comment_path(@comment) %> |
<%= link_to 'Back', comments_path %>
